---
title: "2:58:13 AM - November 15, 2025"
date: 2025-11-15T02:58:13.703Z
timestamp: 1763175493703
---

## Project Notes

**Lists Tab Implementation - Part 2: Navigation**

Fixed navigation for both list types:

1. **CuratedListFeedScreen** (video playlists):
   - Shows video grid from curated list
   - Tap video → fullscreen player
   - Back button overlay to exit video mode
   - Uses `ExploreVideoScreenPure` for playback
   - Fetches video IDs from list via `curatedListVideosProvider`
   - Filters `videoEventsProvider` to show only list videos

2. **UserListPeopleScreen** (people lists):
   - Shows list of profiles using `UserProfileTile`
   - Tap profile → navigate to profile page via `context.goProfile(pubkey)`
   - Simple, clean list view with proper dark theme

3. **Navigation Pattern**:
   - Used `Navigator.push` with `MaterialPageRoute` for both screens
   - Lists tab → tap list card → navigate to detail screen
   - Detail screen has back button to return to lists

**Errors Fixed**:
- Removed invalid `onExit` parameter from ExploreVideoScreenPure
- Fixed UserProfileTile - only needs `pubkey`, not `profile`
- Removed incorrect profile cache provider import

**Still TODO**:
- List discovery (fetch public lists from other users)
- Empty state shown because no lists exist yet in test account
