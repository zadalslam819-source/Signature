# ABOUTME: CI workflow for the videos_repository package.
# ABOUTME: Runs tests, formatting, and analysis only when videos_repository files change.

name: Videos Repository CI

on:
  push:
    branches: [main]
    paths:
      - "mobile/packages/videos_repository/**"
      - ".github/workflows/videos_repository.yaml"
  pull_request:
    branches: [main]
    paths:
      - "mobile/packages/videos_repository/**"
      - ".github/workflows/videos_repository.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      working_directory: "mobile/packages/videos_repository"
