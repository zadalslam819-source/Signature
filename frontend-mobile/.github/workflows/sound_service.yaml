# ABOUTME: CI workflow for the sound_service package.
# ABOUTME: Runs tests, formatting, and analysis only when sound_service files change.

name: Sound-Service CI

on:
  push:
    branches: [main]
    paths:
      - "mobile/packages/sound_service/**"
      - ".github/workflows/sound_service.yaml"
  pull_request:
    branches: [main]
    paths:
      - "mobile/packages/sound_service/**"
      - ".github/workflows/sound_service.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      working_directory: "mobile/packages/sound_service"
