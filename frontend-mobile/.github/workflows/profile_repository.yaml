# ABOUTME: CI workflow for the profile_repository package.
# ABOUTME: Runs tests, formatting, and analysis only when profile_repository files change.

name: Profile Repository CI

on:
  push:
    branches: [main]
    paths:
      - "mobile/packages/profile_repository/**"
      - ".github/workflows/profile_repository.yaml"
  pull_request:
    branches: [main]
    paths:
      - "mobile/packages/profile_repository/**"
      - ".github/workflows/profile_repository.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      working_directory: "mobile/packages/profile_repository"
