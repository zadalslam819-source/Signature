steps:
  # Run database migrations BEFORE deploying new code
  # Skips 0001 (initial schema, not idempotent) - assumes DB already initialized
  # Migrations 0002+ are idempotent and safe to re-run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'run-migrations'
    entrypoint: bash
    args:
      - -c
      - |
        set -e
        echo "📦 Installing PostgreSQL client..."
        apt-get update -qq && apt-get install -y -qq postgresql-client curl > /dev/null

        echo "🔌 Downloading Cloud SQL Proxy..."
        curl -sL -o /tmp/cloud-sql-proxy https://storage.googleapis.com/cloud-sql-connectors/cloud-sql-proxy/v2.14.3/cloud-sql-proxy.linux.amd64
        chmod +x /tmp/cloud-sql-proxy

        echo "🔑 Getting database credentials from Secret Manager..."
        DATABASE_URL=$$(gcloud secrets versions access latest --secret="keycast-database-url")

        # Extract password from DATABASE_URL (format: postgres://user:pass@host/db)
        DB_PASS=$$(echo "$$DATABASE_URL" | sed -n 's|.*://[^:]*:\([^@]*\)@.*|\1|p')
        DB_USER="postgres"
        DB_NAME="keycast"

        echo "🔌 Starting Cloud SQL Proxy..."
        /tmp/cloud-sql-proxy openvine-co:us-central1:keycast-db-plus --port 5432 --gcloud-auth --quiet &
        PROXY_PID=$$!

        # Wait for proxy to be ready
        for i in $$(seq 1 30); do
          if pg_isready -h 127.0.0.1 -p 5432 > /dev/null 2>&1; then
            echo "✅ Cloud SQL Proxy ready"
            break
          fi
          sleep 1
        done

        echo "🚀 Running database migrations..."
        export PGPASSWORD="$$DB_PASS"

        # Skip 0001 (initial schema) - it's not idempotent and should only run once
        # Migrations 0002+ are designed to be safe to re-run
        for migration in database/migrations/000[2-9]*.sql database/migrations/00[1-9][0-9]*.sql; do
          [ -f "$$migration" ] || continue
          echo "  → $$(basename $$migration)"
          psql -h 127.0.0.1 -U $$DB_USER -d $$DB_NAME -f "$$migration" -q 2>&1 | grep -v "already exists" || true
        done

        echo "✅ Migrations complete!"
        kill $$PROXY_PID 2>/dev/null || true

  # Build the Docker image
  # Note: VITE_DOMAIN and VITE_ALLOWED_PUBKEYS are injected at runtime via APP_URL and ALLOWED_PUBKEYS env vars
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--build-arg'
      - 'BUILD_VERSION=${BUILD_ID}'
      - '-t'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/docker/keycast:latest'
      - '.'
    timeout: 1200s

  # Push the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/docker/keycast:latest'

  # Deploy to Cloud Run with hashring-enabled horizontal scaling
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'beta'
      - 'run'
      - 'deploy'
      - 'keycast'
      - '--image=us-central1-docker.pkg.dev/${PROJECT_ID}/docker/keycast:latest'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=3000'
      - '--cpu=4'
      - '--memory=4Gi'
      - '--timeout=300'
      - '--min-instances=3'
      - '--max-instances=200'
      - '--concurrency=50'
      - '--no-cpu-throttling'
      - '--execution-environment=gen2'
      - '--cpu-boost'
      - '--session-affinity'
      - '--use-http2'
      - '--startup-probe=httpGet.path=/health,httpGet.port=3000,initialDelaySeconds=0,periodSeconds=3,failureThreshold=20,timeoutSeconds=2'
      - '--liveness-probe=httpGet.path=/health,httpGet.port=3000,periodSeconds=30,timeoutSeconds=5,failureThreshold=3'
      - '--add-cloudsql-instances=openvine-co:us-central1:keycast-db-plus'
      - '--network=default'
      - '--subnet=default'
      - '--vpc-egress=private-ranges-only'
      - '--set-secrets=DATABASE_URL=keycast-database-url:latest,SERVER_NSEC=keycast-ucan-secret:latest,SENDGRID_API_KEY=keycast-sendgrid-api-key:latest,REDIS_URL=keycast-redis-url:latest'
      - '--set-env-vars=^;^NODE_ENV=production;USE_GCP_KMS=true;GCP_PROJECT_ID=${PROJECT_ID};ALLOWED_ORIGINS=https://login.divine.video,https://divine.video;APP_URL=https://login.divine.video;FROM_EMAIL=noreply@divine.video;RUST_LOG=info;ENABLE_EXAMPLES=true;SQLX_STATEMENT_CACHE=100;SQLX_POOL_SIZE=50;ALLOWED_PUBKEYS=89ef92b9ebe6dc1e4ea398f6477f227e95429627b0a33dc89b640e137b256be5,76c71aae3a491f1d9eec47cba17e229cda4113a0bbb6e6ae1776d7643e29cafa;BUNKER_RELAYS=wss://relay.divine.video,wss://relay.primal.net,wss://relay.nsec.app,wss://nos.lol;ENABLE_DIVINE_NAMES=true'

  # Smoke tests - verify deployment
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: bash
    args:
      - -c
      - |
        echo "⏳ Waiting for service to be ready..."
        sleep 30

        SERVICE_URL=$$(gcloud run services describe keycast --region=us-central1 --format='value(status.url)')
        echo "Service URL: $$SERVICE_URL"

        echo "🔍 Testing API health endpoint..."
        if ! curl -f -s "$$SERVICE_URL/health"; then
          echo "❌ Health check failed"
          exit 1
        fi
        echo "✅ Health check passed"

        echo "🔍 Testing CORS preflight for /api/auth/register..."
        HTTP_CODE=$$(curl -s -o /dev/null -w "%{http_code}" \
          -H "Origin: https://login.divine.video" \
          -H "Access-Control-Request-Method: POST" \
          -H "Access-Control-Request-Headers: Content-Type" \
          -X OPTIONS \
          "$$SERVICE_URL/api/auth/register")

        if [ "$$HTTP_CODE" != "200" ]; then
          echo "❌ CORS preflight failed with HTTP $$HTTP_CODE"
          exit 1
        fi
        echo "✅ CORS preflight passed"

        echo "🎉 All smoke tests passed!"

images:
  - 'us-central1-docker.pkg.dev/${PROJECT_ID}/docker/keycast:latest'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

timeout: 2400s
